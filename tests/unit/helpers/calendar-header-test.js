import {
  calendarHeader
} from '../../../helpers/calendar-header';
import { module, test } from 'qunit';

module('CalendarHeaderHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = calendarHeader(42);
  assert.ok(result);
});
